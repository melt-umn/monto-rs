version: '3'
services:
  broker:
    build:
      context: .
      dockerfile: dockerfiles/broker.Dockerfile
    ports:
     - "28888:28888"
  cc:
    build:
      context: .
      dockerfile: dockerfiles/ableC-cpp.Dockerfile
  loctrans:
    build:
      context: .
      dockerfile: dockerfiles/loctrans.Dockerfile
